###############################################
# ENVIRONMENT VARIABLES
# Sudah diisi dengan default credentials
# Token akan auto-update setelah login
###############################################

### Base URL
@baseUrl = http://localhost:5000

### Default Credentials
# PENTING: Admin dan Customer SEMUA menggunakan PHONE NUMBER + PASSWORD

# Admin (sudah ada dari seeder)
@adminPhone = 082111111111
@adminPassword = admin123
@adminName = Super Administrator

# Customer (buat dulu via register)
@customerPhone = 08123456789
@customerPassword = password123
@customerName = Customer Test

### Tokens (auto-update setelah login via response)
@customerToken = 
@adminToken = 

### Sample IDs (copy dari response API)
@productId = 
@categoryId = 
@discountId = 
@cartItemId = 
